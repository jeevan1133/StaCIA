A2|What is the website link for [CSSESTATClub]?|The URL is {CSSESTATClub}.\tThe {website} doesn't seem to have a url.|SELECT `website` FROM `club` WHERE `cname` LIKE '%{}%';
A2|Is there a website for [CSSESTATClub]?|The URL is {CSSESTATClub}.\tThe {website} doesn't seem to have a url.|SELECT `website` FROM club WHERE `cname` LIKE '%{}%';
A2|Does [CSSESTATClub] has a website?|The URL is {CSSESTATClub}.\tThe {website} doesn't seem to have a url.|SELECT `website` FROM club WHERE `cname` LIKE '%{}%';
A2|How do I learn more about [CSSESTATClub]?|The URL for {cname} is {website}.\tThe {CSSESTATClub} doesn't seem to have a url.|SELECT `cname`, `website` FROM `club` WHERE `cname` LIKE '%{}%';
A2|Does [CSCSTAT] tutoring cost anything?|It costs {cost}|SELECT `cost` FROM `tutor`;
A2|What is the cost of [CSCSTAT] tutoring?|It costs {cost}|SELECT `cost` FROM `tutor`;
A2|Can I get price for [CSCSTAT] tutoring?|It costs {cost}|SELECT `cost` FROM `tutor`;
A2|Do I need to pay for [CSCSTAT] tutoring?|It costs {cost}|SELECT `cost` FROM `tutor`;
A2|Is there free [CSCSTAT] tutoring?|It costs {cost}|SELECT `cost` FROM `tutor`;
A2|How much does [CSCSTAT] tutoring cost?|It costs {cost}|SELECT `cost` FROM `tutor`;
A2|Is [CSCSTAT] tutoring free?|It costs {cost}|SELECT `cost` FROM `tutor`;
A2|What is the email address to contact to apply to be a CSC tutor?|{data}|SELECT `data` FROM `tutorExtraDump` WHERE `department` = \"CSSE\" AND `keywords` = \"Want to be a Tutor?\";
A2|Where should I email if I want to be a CSC tutor?|{data}|SELECT `data` FROM `tutorExtraDump` WHERE `department` = \"CSSE\" AND `keywords` = \"Want to be a Tutor?\"
A2|How can you be a CSC tutor?|{data}|SELECT `data` FROM `tutorExtraDump` WHERE `department` = \"CSSE\" AND `keywords` = \"Want to be a Tutor?\"
A2|Who should I contact to become a CSC tutor?|{data}|SELECT `data` FROM `tutorExtraDump` WHERE `department` = \"CSSE\" AND `keywords` = \"Want to be a Tutor?\";
A2|Who do I contact to be a CSC tutor?|{data}|SELECT `data` FROM `tutorExtraDump` WHERE `department` = \"CSSE\" AND `keywords` = \"Want to be a Tutor?\";
A2|Which CSC tutors are available on [Day]?|Available tutors are {name}.|SELECT DISTINCT `name` FROM `tutor` NATURAL JOIN `tutorSchedule` WHERE `dayOfTheWeek` = \"{}\";
A2|Who is available to tutor on [Day]?|Available tutors are {name}|SELECT DISTINCT `name` FROM `tutor` NATURAL JOIN `tutorSchedule` WHERE `dayOfTheWeek` = \"{}\";
A2|Is [Tutorname] going to be tutoring CSC on [Day]?|{name} is tutoring on {dayOfTheWeek}.|SELECT DISTINCT `name`, `dayOfTheWeek` FROM tutor NATURAL JOIN `tutorSchedule` WHERE `name`=\"%s\" and `dayOfTheWeek` =\"{}\";
A2|Who will be tutoring on [Day]?|{name} is tutoring on {dayOfTheWeek}.|SELECT DISTINCT `name`, `dayOfTheWeek` FROM tutor NATURAL JOIN `tutorSchedule` WHERE `name`= \"%s\" and `dayOfTheWeek` =\"{}\";
A2|Where is [CSCSTAT] tutoring?|It's on {location}|SELECT DISTINCT `location` FROM `tutorSchedule`;
A2|Is [person] tutoring on [day]?|{name} is tutoring on {dayOfTheWeek}.|SELECT DISTINCT `name`,`dayOfTheWeek` FROM tutor NATURAL JOIN `tutorSchedule` WHERE `name` = \"%s\" and `dayOfTheWeek` =\"{}\";
A2|Is [tutor] available on [day]?|{tutor} is tutoring on {day}.|SELECT DISTINCT `name`,`dayOfTheWeek` FROM tutor NATURAL JOIN `tutorSchedule` WHERE `name`=\"{}\" and `dayOfTheWeek` =\"{}\";
A2|Who is available to tutor on [day]?|{name} is tutoring on {dayOfTheWeek}.|SELECT DISTINCT `name`, `dayOfTheWeek` FROM tutor NATURAL JOIN `tutorSchedule` WHERE `.name` = \"{}\" and `dayOfTheWeek` =\"{}\";
A2|Which skills do the CSC tutors help with?|They help you figure out course material that you're having trouble understanding on your own. This material may include: Basics of programming in Python, Java, or C Debugging techniques and independent problem solving Data structures Unix shell commands Text editor operation.|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"What We Will Do\";
A2|What can I get help with at CSC tutoring?|They help you figure out course material that you're having trouble understanding on your own. This material may include: Basics of programming in Python, Java, or C Debugging techniques and independent problem solving Data structures Unix shell commands Text editor operation.|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"What We Will Do\";
A2|How do CSC tutors help me?|They help you figure out course material that you're having trouble understanding on your own. This material may include: Basics of programming in Python, Java, or C Debugging techniques and independent problem solving Data structures Unix shell commands Text editor operation.|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"What We Will Do\";
A2|What will CSC tutors do for me?|They help you figure out course material that you're having trouble understanding on your own. This material may include: Basics of programming in Python, Java, or C Debugging techniques and independent problem solving Data structures Unix shell commands Text editor operation.|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"What We Will Do\";
A2|Do CSC tutors help me with [programming language]?|They can help you with  Basics of programming in Python, Java, or C Debugging techniques and independent problem solving Data structures Unix shell commands Text editor operation.|SELECT * FROM `tutorExtraDump` WHERE `keywords` = \"What We Will Do\";
A2|What can I expect at the CSC tutoring session?|They can help you with  Basics of programming in Python, Java, or C Debugging techniques and independent problem solving Data structures Unix shell commands Text editor operation.|SELECT * FROM `tutorExtraDump` WHERE `keywords` = \"What We Will Do\";
A2|What does [CSSESTATClub] do?|{clubDescription}\tCalPoly doesn't have that club in CSSE or STAT department.|SELECT `clubDescription` FROM `club` WHERE `cname` LIKE '%{}%';
A2|Can you describe [CSSESTATClub]?|{clubDescription}\tCalPoly doesn't have that club in CSSE or STAT department.|SELECT `clubDescription` FROM `club` WHERE `cname` LIKE '%{}%';
A2|What does [CSSESTATClub] description say?|{clubDescription}\tCalPoly doesn't have that club in CSSE or STAT department.|SELECT `clubDescription` FROM `club` WHERE `cname` LIKE '%{}%';
A2|Can I get tutoring in [STATCLASSNAME]?|The link to STAT classes is {data}.|SELECT `data` FROM `tutorExtraDump` WHERE `department` = \"{}\" AND `keywords` = \"{}\";
A2|Is tutoring available for [STATCLASSNAME]?|The link to STAT classes is {data}.|SELECT `data` FROM `tutorExtraDump` WHERE `department` = \"{}\" AND `keywords` = \"{}\";
A2|Do STAT tutors help me with [STATCLASSNAME]?|The link to STAT classes is {data}.|SELECT `data` FROM `tutorExtraDump` WHERE `department` = \"{}\" AND `keywords` = \"{}\";
Does [CSSESTATClub] have an advisor?|{advisorName} is advising {cname}\t{CSSESTATClub} doesn't have an advisor.|SELECT  `advisorName`, `cname` FROM `clubAdvisorContact` NATURAL JOIN `club`  WHERE `cname` LIKE '%{}%'; 
Who is the advisor for [CSSESTATClub]?|{advisortName} advises {cname}.\t{CSSESTATClub} desn't have an advisor.|SELECT  `advisorName`, `cname` FROM `clubAdvisorContact` NATURAL JOIN `club`  WHERE `cname` LIKE '%{}%'; 
A2|Is [Professor] advising [CSSESTATClub]?|{advisorName} is advising {cname}.\tProfessor {advisorName} is not advising {}.|SELECT `advisorName`, `cname` club FROM `clubAdvisorContact` NATURAL JOIN `club` WHERE `advisorName` LIKE '%{}%';
A2|Who is advising [CSSESTATClub]?|{advisorName} is advising {cname}\t{CSSESTATClub} doesn't have an advisor.|SELECT  `advisorName`, `cname` FROM `clubAdvisorContact` NATURAL JOIN `club`  WHERE `cname` LIKE '%{}%';
A2|Who are the advisers for [CSCSTATClub]?|{}\tThere are none.|SELECT `advisorName` FROM `clubAdvisorContact` WHERE `cname` LIKE '%{}%';
A2|Who are the current advisers for the [CSSESTATClub]?|The current advisers are: {advisorName}\t{CSSESTATClub} doesn't have any current advisers.|SELECT `advisorName` FROM `clubOfficers` WHERE `department` = \"{}\";
A2|Does [CSCSTATClub] have any advisers?|{advisorName} is advising {cname}\t{CSSESTATClub} doesn't have any advisers.|SELECT  `advisorName`, `cname` FROM `clubAdvisorContact` NATURAL JOIN `club`  WHERE `cname` LIKE '%{}%';
A2|What is the location of [CSSESTATClub]?|Their location is {data}.\tThe club {CSSESTATClub}doesn't have any location.|SELECT `data` FROM `clubExtraDump` WHERE `keywords` = 'Location' AND `cname` LIKE '%{}%';
A2|Where is [CSSESTATClub]?|{data}\tClub {CSSESTATClub} doesn't have any location.|SELECT `data` FROM `clubExtraDump` WHERE `keywords` = 'Location' AND `cname` LIKE '%{}%';
A2|Where is [CSSESTATClub] located at?|{data}\t Club {CSSESTATClub} doesn't have any location.|SELECT `data` FROM `clubExtraDump` WHERE `keywords` = 'Location' AND `cname` LIKE '%{}%';
A2|What CSC classes can I get help in at general tutoring?|{name}|SELECT `name` FROM `tutorClasses`;
A2|How can you help me with [CSSE] tutoring?|{name}|SELECT `name` FROM `tutorClasses`;
A2|Can I get help with [CSCClass]?|Yes you can get help with {name}.\tThey can't help you with {CSCClass}.|SELECT `name` FROM `tutorClasses` where `name` = \"{}\";
A2|Do they tutor [CSCClass]?|Yes you can get help with {name}\tThey can't help you with {CSCClass}.|SELECT `name` FROM `tutorClasses` where `name` = \"{}\";
A2|What classes can the CSC tutors help me in?|{name}|SELECT `name` FROM `tutorClasses`;
A2|Can I get CSC help if I'm in [CSCClassName]?|{name}|SELECT `name` FROM `tutorClasses`;
A2|What does [TutorName] answer?|{data} doesn't have any contact information.\tYou can get more information at {data}|SELECT `data` FROM `tutorExtraDump` NATURAL JOIN `tutor` WHERE `department` = \"CSSE\" and `name` = \"{}\";\tSELECT `data` FROM `tutorExtraDump` WHERE `department` = \"STAT\" and keywords = \"Tutors\"
A2|Can I get help with [CSCSTATClassName]|CSC tutors can help you with CSC {name}\tYou can find more information on|SELECT `name` FROM `tutorClasses` WHERE `department` = \"{}\";\tSELECT `data` FROM `tutorExtraDump` WHERE `keywords` = `Tutors`;
A2|Where can I visit [CSSESTATClub] online?|The URL is {website}.\tThe Club {CSSESTATClub} doesn't have any website.|SELECT `website` FROM `club` WHERE `cname` LIKE '%{}%';
A2|What is the URL for [CSSESTATClub]?|The URL is {website}.\tThe Club {CSSESTATClub} doesn't have any website.|SELECT `website` FROM `club` WHERE `cname` LIKE '%{}%';
A2|What time does [CSCSTAT] tutoring start?|{data}\tYou can find more information on {data}.|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Time\" and `department` = \"{}\";\tSELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Tutor Hours\" and `department` = \"{}\".
A2|Which room is [CSCSTAT] tutoring located?|{data}\tYou can find more information at {data}.|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Building and Room\" and `department` = \"{}\";\tSELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Tutors\" and `department` = \"{}\"
A2|What does [CSCSTATTutoring] schedule look like?|{data}\tYou can find more information on {data}.|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Building and Room\" and `department` = \"{}\";\tSELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Tutors\" and `department` = \"{}\";
A2|Can the [CSCSTAT] tutors help me with homeworks?|{data}\tYou can find more information at {data}.|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"What We Won't Do\" and `department` = \"{}\";\tSELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Tutor Hours\" and `department` = \"{}\";
A2|What are the things that [CSCSTAT] tutors won't do?|{data}\tYou can find more information at {data}.|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"What We Won't Do\" and `department` = \"{}\";\tSELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Tutor Hours\" and `department` = \"{}\";
A2|Can you describe the [CSSESTATClub]?|From {cname}'s homepage, it says {clubDescription}.\t{CSSESTATClub} doesn't seem to be in CSSE or STAT clubs.|SELECT `cname`, `clubDescription` FROM `club` WHERE `club` = \"{}\";
A2|Could you give me a brief overview of [CSSESTATClub]?|From {cname}'s homepage, it says {clubDescription}.\t{CSSESTATClub} doesn't seem to be in CSSE or STAT clubs.|SELECT `cname`, `clubDescription` FROM `club` WHERE `club` = \"{}\";
A2|Is tutoring provided for [STATCLASSNAME]?|The link to STAT classes is {data}.|SELECT `data` FROM `tutorExtraDump` WHERE `department` = \"{}\" AND `keywords` = \"Tutors\";
A2|Can I get help with [CSCSTATCLASSNAME]|{}\tThe link to STAT classes is {data}.|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"CS Tutoring Services\" and `department` = \"{}\";\tSELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Tutors\" and `department` = \"{}\";
A2|Which classes will be covered in general CSC tutoring?|{data}|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"CS Tutoring Services\"; 
A2|What classes should I be enrolled in to get good help during CSC tutoring?|{data}|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"CS Tutoring Services\";
A2|What should I do if 14-302 tutoring is unavailable?|Tutoring will be held in the adjacent lab.|SELECT DISTINCT `department` FROM `tutorExtraDump`;
A2|What should I do if tutoring building is unavailable?|Tutoring will be held in the adjacent lab.|SELECT DISTINCT `department` FROM `tutorExtraDump`;
A2|At what point does [CSCSTATTutoring start] in the quarter?|{data}|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Days\" AND `department` = \"{}\";\tSELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Tutor Hours\" AND `department` = \"STAT\";
A2|What are the events that SLOHACKS organization does using the quarter?|{data}|SELECT `event`  FROM `clubSchedule` WHERE `cname` = \"SLO Hacks\";
A2|What are the upcoming events for [CSCSTATOrg]|Upcoming events for {cname} are {event}\tThere are no upcoming events scheduled for {event}.|SELECT `cname` , `event`  FROM `clubSchedule` WHERE `cname` = \"SLO Hacks\";
A2|What's [CSCSTAT] tutoring office hours?|{data}|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = `Time` and `department` = \"{}\";\tSELECT `data` FROM `tutorExtraDump` WHERE `keywords` = `Tutor Hours` and `department` = \"STAT\";
A2|Can you reserve a tutor for [CSCSTAT]?|You can reserve one for STAT, not for CSSE|SELECT `data` FROM `tutorExtraDump`;
A2|Does [CSCSTAT] have private tutoring?|{data}\t{CSCSTAT} don't have private tutoring.|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = `Free Statistics Tutoring -` and `department` = \"{}\";\tSELECT `data` FROM `tutorExtraDump` WHERE `keywords` = `CS Tutoring Services` and `department` = \"{}\";
A2|What are some of the [CSCSTAT] clubs?|They are {cname}.|SELECT `cname` FROM `club` WHERE `department` = \"{}\";
A2|Are there any upcoming events for [CSCSTAT] clubs?|The events for {cname} are {event}\t{}|SELECT `cname`, `event` FROM` clubSchedule WHERE `cname` LIKE '%{}%'\tSELECT `data` FROM `clubExtraDump` WHERE `keywords` = \"Events\" AND `cname` LIKE '%{}%';
A2|What days are [CSCSTAT] tutors available?|{dayOfTheWeek}\tYou can find more information on {data}|SELECT DISTINCT `dayOfTheWeek` FROM `tutor` NATURAL JOIN `tutorSchedule` WHERE `department` = \"{}\";\tSELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Tutor Hours\";
A2|Which [CSCSTAT] classes can I get tutoring services for?|You can get help with CSC {name}\tYou can find more information at {data}|SELECT `name` FROM `tutorClasses` WHERE `department` = \"{}\";\tSELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Tutors\"; 
A2|Does CalPoly have [CSCSTAT] club?|Yes, CalPoly has {cname} club.\tCalPoly doesn't seem to have {CSCSTAT} club.|SELECT `cname` FROM `club` WHERE `cname` LIKE `%{}%`;
A2|Can I be tutored outside of class?|{data}|SELECT `data` FROM `tutroExtraDump` WHERE `keywords` = `CS Tutoring Services`;
A2|Can CSC tutors help me outside of school?|{data}|SELECT `data` FROM `tutroExtraDump` WHERE `keywords` = `CS Tutoring Services`;
A2|Can CSC tutors meet me outside to be tutored?|{data}|SELECT `data` FROM `tutroExtraDump` WHERE `keywords` = `CS Tutoring Services`;
A2|Who is the head tutor of CSSE?|Nicole Anderson-Au Class of 2020, nander17@calpoly.edu|SELECT `name` FROM `tutor`;
A2|How many tutors are there in CSSE department?|There are {name} tutors available.|SELECT COUNT(`name`) FROM `tutorClasses`;
A2|Is [TutorName] a CSC tutor?|{name} is a CSC tutor.\t[TutorName] is not a CSC tutor.|SELECT `name` FROM `tutor` WHERE `name` LIKE '%{}%';
A2|Who is available to tutor me on [days] ?|The available tutors are {name}.\tNone of the tutors are available on {days} day.|SELECT DISTINCT `name` FROM `tutor` NATURAL JOIN `tutorSchedule` WHERE `dayOfTheWeek` = \"{}\";
A2|Where is the [CSCSTAT] tutoring center?|{data}|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Building and Room\" AND `department` = \"{}\";\tSELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Tutor Hours\" AND `department` = \"STAT\";
A2| What's [CSCSTAT] club's email address?|It is {generalEmail}.\tCalPoly doesn't seem to have {CSCSTAT} club.|SELECT `generalEmail` FROM `club` WHERE `cname` LIKE '%{}%';
A2|Who is the current president of [CSCSTATClub]?|The president for {cname} is {name}.\tClub {} doesn't have any president.|SELECT `cname`, `name` FROM `clubOfficers` WHERE `title` = \"President\" and `cname` LIKE '%{}%';
A2|What projects does [CSSESTATClub] have?|You can get involved in {data}\tThey don't have any projects updated yet.|SELECT `data` FROM `clubExtraDump` WHERE `keywords` = \"Projects\" AND `cname` LIKE '%{}%';
A2|When does [CSSESTATClub] club meet?|{data}\tThey don't have it posted. You may want to try again later.|SELECT `data` FROM `clubExtraDump` WHERE `cname` LIKE '%{}%';
A2|If I want to get Linux on my Computer who can help me?|You should contact {cname}\tContact CPGD for more information.|SELECT `cname` FROM `club` WHERE `clubDescription` LIKE '%{}%';;
A2|When does [CSSEClubOrgName] meet?|They meet on {data}\tThey don't have it posted yet. You may want to try again later.|SELECT `data` FROM `tutorExtraDump` WHERE `cname` LIKE '%{}%' AND `keywords` = \"Meeting\";
A2|Where does [CSSESTATClub] meet?|They meet at {data}\tThey don't have it posted. You may want to try again later.|SELECT `data` FROM `tutorExtraDump` WHERE `cname` '%{}%' AND `keywords`=\"Location\";
A2|When does [CSSESTATClub] meet?|They meet on {data}\tThey don't have it posted. You may want to try again later.|SELECT `data` FROM `tutorExtraDump` WHERE `cname` LIKE '%{}%' AND `keywords` = \"Meeting\";
A2|What room does [CSSESTATClub] meet?|They meet at {data}\tThey don't have it posted. You may want to try again later.|SELECT `data` FROM `tutorExtraDump` WHERE `cname` LIKE '%{}%' AND `keywords` = \"Location\";
A2|Where does [CSSEClubOrgName] meet?|{data}\tThey don't have it posted. You may want to try again later.|SELECT `data` FROM `tutorExtraDump` WHERE `cname` LIKE '%{}%';
A2|Who is the [OfficerRole] of [CSSESTATClub]|The {title} of {cname} is {name}\t{CSSESTATClub} doesn't have any officer for that role.|SELECT `title`, `cname`, `name` FROM `clubOfficers` WHERE `title` = \"{}\" AND `department` = \"{}\";
A2|What clubs does [CSCSTAT] have?|{cname}\t{CSCSTAT} doesn't seem to be in CSC or STAT department.|SELECT `cname` FROM `clubs` WHERE `department` = \"{}\";
A2|Who are the current [CSCSTAT] tutors?|Current tutors are {name}|SELECT `name` FROM `tutor`;
A2|How can I sign up to be paired with a Stat Club mentor?|The form for signing up for a mentor is {data}|SELECT `data` FROM `clubExtraDump` WHERE `cname` = \"STAT Club\" AND `keywords` = \"signup_for_classes\";
A2|Where is the form for requesting a mentor for the Stat Club?|The form for signing up for a mentor is {data}|SELECT `data` FROM `clubExtraDump` WHERE `cname` = \"STAT Club\" AND `keywords` = \"signup_for_classes\";
A2|How can I sign up to be a mentor with the Stat Club?|The form for signing up to be a mentor is {data}|SELECT `data` FROM `clubExtraDump` WHERE `cname` = \"STAT Club\" AND `keywords` = \"signup_for_mentor\";
A2|Where is the form for offering to be a mentor with the Stat Club?|The form for signing up to be a mentor is {data}|SELECT `data` FROM `clubExtraDump` WHERE `cname` = \"STAT Club\" AND `keywords` = \"signup_for_mentor\"
A2|Who is the [StatYear] President for the [CSSESTATClub]?|The president for {term} term is {name}.\tThey don't have it updated.|SELECT `term`, `name` FROM `clubOfficers` WHERE LIKE '%{}%' AND `department` = \"STAT Club\";
A2|Who is the [StatYear] Vice-President for the [CSSESTATClub]?|The Vice-President for {term} term is {name}.\tThey don't have it updated.|SELECT `term`, `name` FROM `clubOfficers` WHERE LIKE '%VICE%' AND `department` = \"STAT Club\";
A2|Who is the [StatYear] Secretary for the [CSSEStatClub]?|The Secretary for {term} term is {name}.\tThey don't have it updated.|SELECT `term`, `name` FROM `clubOfficers` WHERE LIKE '%Secretary%' AND `department` = \"{}\";
A2|Who is the [StatYear] Treasurer for the [CSSESTATClub]?|The Treasurer for {term} term is {name}.\tThey don't have it updated.|SELECT `term`, `name` FROM `clubOfficers` WHERE LIKE '%Treasurer%' AND `department` = \"{}\";
A2|Who is the [StatYear] Events Coordinator for the [CSSESTATClub]?|The Events Coordinator for {term} term is {name}.\tThey don't have any.|SELECT `term`, `name` FROM `clubOfficers` WHERE LIKE '%Events%' AND `department` = \"{}\";
A2|Who is the [StatYear] COSAM Representative for the Stat Club?|The COSAM Representative for {term} term is {name}.\tThey don't have any.|SELECT `term`, `name` FROM `clubOfficers` WHERE LIKE '%COSAM%' AND `department` = \"{}\";
A2|Who is the [StatYear] Club Liaison for the [CSSEStatClub]?|The Liaison for {term} term is {name}.\tThey don't have any.|SELECT `name` FROM `clubOfficers` WHERE LIKE '%Lia%' AND `department` = \"{}\";
A2|Who is the [StatYear] Open House Officer for the [CSSEStatClub]?|The Open House Officer for {term} term is {name}.\tThey don't have any.|SELECT `term`, `name` FROM `clubOfficers` WHERE LIKE '%Open%' AND `department` = \"{}\";
A2|What is the general contact information for [CSSESTATClub]?|The email is {generalEmail}.\t{CSSESTATClub} doesn't have any email.|SELECT `generalEmail` FROM `club` WHERE `cname` LIKE '%{}%';
A2|Who is the current [OfficerRole] for [CSSESTATClub]|The current {title} for {cname} is {name}.\t{CSSESTATClub} isn't in {}.|SELECT `title` , `cname`, `name` FROM clubOfficers WHERE `title` = \"{}\" AND `cname` LIKE '%{}%';
A2|What time does [CSCSTAT] tutoring meet?|{data}\tYou can find more information on {data}|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Time\" AND `department` = \"{}\";\tSELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Tutor Hours\" AND `department` = \"{}\";
A2|Who are some private tutors for Statistics?|Here is a PDF for a list of private Statistics tutors {data}|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Tutors\" AND `department` = \"STAT\";
A2|Does [CSCSTAT] have private tutors?|Yes they have private tutors. More information is available on {data}\tCSC doesn't have private tutors.|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Tutors\" AND `department` = \"{}\";
A2|What do I need to do to be a [CSCSTAT] tutor?|{}\tYou can signup at {data} to be a STAT Tutor.|SELECT `data` FROM `tutorExtraDump` WHERE `department` = \"{}\" AND `keywords` = \"Want to be a Tutor?\";\tSELECT `data` FROM `clubExtraDump` WHERE `cname` LIKE  \"%{}%\"
A2|Does [CSSESTATClub] have a [OfficerRole] position?|Yes {title} has {title}\tNo, {CSSESTATClub} doesn't have {OfficerRole}.|SELECT `cname`, `title` FROM `clubOfficers` WHERE `cname` LIKE '%{}%'AND `title` LIKE '%{}%';
A2|Is there a [OfficerRole] position in [CSSESTATClub]?|Yes {CSSESTATClub}  has {OfficerRole}\t{OfficerRole} isn't in {CSSESTATClub}.|SELECT `cname`, `title` FROM `clubOfficers` WHERE `title` LIKE '%{}%' AND `cname` LIKE '%{}%';
A2|Does [CSSESTATClub] have a [OfficerRole] leadership position?|Yes {cname} has {title}\t{CSSESTATClub} doesn't have {OfficerRole}.|SELECT `cname`, `title` FROM `clubOfficers` WHERE `cname` LIKE '%{}%' AND `title` LIKE '%{}%';
A2|Could you give me a list of the private STAT tutors?|Here is a PDF for a list of private Statistics tutors {data}|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Tutors\" AND `department` = \"STAT\";
A2|Who are all of the private tutors for STAT?|Here is a PDF for a list of private Statistics tutors {data}|SELECT `data` FROM `tutorExtraDump` WHERE `keywords` = \"Tutors\" AND `department` = \"STAT\";
